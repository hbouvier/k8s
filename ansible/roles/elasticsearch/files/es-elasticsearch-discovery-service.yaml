apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-discovery
  labels:
    app: elk
    component: elasticsearch
    role: master
spec:
  selector:
    app: elk
    component: elasticsearch
    role: master
  ports:
  - name: transport
    port: 9300
    protocol: TCP
